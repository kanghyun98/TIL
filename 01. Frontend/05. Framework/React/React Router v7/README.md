# React Router v7

> Framework Mode
> 
- `Framework Mode`는 Vite 플러그인을 통해 Data Mode를 감싸는 방식
- 아래 기능들 제공
    - type-safe `href`
    - type-safe Route Module API - `params`, `loaderData`
    - intelligent code splitting
    - SPA, SSR, and static rendering strategies
